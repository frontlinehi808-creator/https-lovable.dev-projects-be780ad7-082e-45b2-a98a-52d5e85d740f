import { createClient } from "npm:@supabase/supabase-js@2.57.4";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"};
const allowed=(path:string,method:string)=>(method==="GET"&&path==="/stores")||(method==="GET"&&/^\/mockup-generator\/printfiles\/\d+$/.test(path))||(method==="POST"&&/^\/mockup-generator\/create-task\/\d+$/.test(path))||(method==="GET"&&path==="/mockup-generator/task");
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response(null,{headers:cors});try{const authHeader=req.headers.get("Authorization");if(!authHeader)throw new Error("Sign in required");const authClient=createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_ANON_KEY")!,{global:{headers:{Authorization:authHeader}}});const{data:{user},error:authError}=await authClient.auth.getUser();if(authError||!user)throw new Error("Invalid or expired sign-in");const token=Deno.env.get("PRINTFUL_API_KEY");if(!token)throw new Error("PRINTFUL_API_KEY is not configured in Supabase Edge Function secrets");const{path,method="GET",payload,params,store_id}=await req.json();const verb=String(method).toUpperCase();if(typeof path!=="string"||!allowed(path,verb))throw new Error("This Printful operation is not allowed");const url=new URL(path.replace(/^\//,""),"https://api.printful.com/");Object.entries(params||{}).forEach(([k,v])=>url.searchParams.set(k,String(v)));const headers:Record<string,string>={Authorization:`Bearer ${token}`,Accept:"application/json","Content-Type":"application/json"};if(store_id)headers["X-PF-Store-Id"]=String(store_id);const upstream=await fetch(url,{method:verb,headers,body:payload&&verb==="POST"?JSON.stringify(payload):undefined});const data=await upstream.json().catch(()=>({error:"Non-JSON response from Printful"}));return Response.json({status:upstream.status,data},{headers:cors})}catch(e){return Response.json({error:(e as Error).message},{status:400,headers:cors})}});